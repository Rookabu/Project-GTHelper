{
  "private": true,
  "type" : "module",
  "scripts": {
    "start": "dotnet tool restore && dotnet fable watch src --noCache --runFast vite",
    "build": "dotnet tool restore && dotnet fable src --run vite build --emptyOutDir",
    "clean": "dotnet fable clean src --yes"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^3.1.0",
    "autoprefixer": "^10.4.18",
    "daisyui": "^4.7.3",
    "postcss": "^8.4.35",
    "tailwindcss": "^3.4.1",
    "vite": "^4.1.0"
  },
  "engines": {
    "node": ">=18"
  }
}
